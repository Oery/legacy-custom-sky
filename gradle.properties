# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Per-version/mod properties (minecraft_version, loader_version, loom_version,
# mod_version, maven_group, fabric_api_version, modmenu_version, ...) now live
# in stonecutter.properties.toml, since they differ per Stonecutter node.
